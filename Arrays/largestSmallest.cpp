#include <iostream>

/*
int main()
{
	int a;
	int b;
	int c;
	int d;
	int e;
	int f;
	int g;
	int h;
	int i;
	int j;

	int smallest;
	int largest;

	printf("Please input 10 numbers seperated by spaces:\n");
	scanf_s("%d %d %d %d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g, &h, &i, &j);

	int saveArray[10] = { a, b, c, d, e, f, g, h, i, j};

	largest = saveArray[0];
	smallest = saveArray[0];

	for (int k = 1; k < 10; k++)
	{
		if (saveArray[k] > largest)
		{
			largest = saveArray[k];
		}
		if (saveArray[k] < smallest)
		{
			smallest = saveArray[k];
		}
	}

	printf("\nThe largest value is: %d", largest);
	printf("\nThe smallest value is: %d\n", smallest);

	system("pause");
}
*/